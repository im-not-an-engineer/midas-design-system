# 폴리싱 1차 — 다음 세션 메모

작성 2026-09-23. 브랜치 `폴리싱-1차`, 커밋 31개 전부 푸시됨.
**PR 은 아직 안 만들었다.**

https://github.com/im-not-an-engineer/midas-design-system/pull/new/%ED%8F%B4%EB%A6%AC%EC%8B%B1-1%EC%B0%A8

## 리뷰에서 먼저 볼 것 — 계약 151 → 154

| 토큰 | 값 | 왜 |
|---|---|---|
| `font.weight.bold` | 700 | primitive 에만 있어 못 쓰던 것. 아직 쓰는 곳 없음(사다리 완성용) |
| `surface.accentSubtle` | accent.50 | 옅은 키 컬러 면. 토글 눌림·호버. 함께 `surface.selected` 을 50 → 100 으로 |
| `font.size.footnote` | 11px | caption 아래 한 단계. 입력칸 아래 설명·에러. consumer 는 12px |
| `border.width.heavy` | 2.5px | 슬라이더 썸. 계약 밖 `border-2` 를 쓰고 있었다 |

전 프리셋 공통이라 yhy0227 확인 필요.

## 알고 넘어간 것

**destructive 버튼(red 500)이 흰 글자 대비 3.76:1 로 WCAG AA 미달.** 기준 4.5:1.
사내 도구 전제로 선명함을 택했다. 문제되면 600 으로 되돌리면 된다.

## 이번에 네 번 나온 함정 — AGENTS.md 에 적을 만함

Tailwind 는 클래스를 적은 순서가 아니라 자기 순서로 정렬한다. 같은 속성에 상태
변형을 나란히 걸면 의도와 반대로 이긴다. 뒤에 적는 것으로는 안 되고 `not-` 로
조건에서 빼야 한다.

  data-selected vs data-highlighted   팝업 항목
  data-pressed  vs data-disabled      토글 · 툴바
  data-active   vs data-disabled      탭

## 남은 일

- [ ] PR 만들기
- [ ] 다이얼로그 기본 포커스 — Apple HIG 처럼 취소에 둘지 미확인
- [ ] 목록 컴포넌트 — 없어서 갤러리가 `<li>` 를 직접 그린다. 제품마다 높이를 각자 고르는 구조
- [ ] 빌드 규칙 8 (치수 단계 순서 `sm < md < lg`) — 프롬프트 초안 있음. 지금은 조용히 통과한다
- [ ] Select·Combobox 아이콘 위치 2px 차이 — 맞출지 미정
- [ ] 계약에 있으나 안 쓰이는 키: `status.danger.border`, `font.weight.regular`, `font.weight.bold`

## 다음 브랜치 — 컴포넌트 추가

폴리싱과 섞지 않으려고 브랜치를 나눈다. 아래는 전부 `packages/react/src/components/`
에 **새로 만드는** 것이다 — 갤러리에만 있으면 제품이 못 가져다 쓴다.

### 먼저 (새 의존성 0)

- [ ] **Badge** — 지금 `gallery.tsx` 에 인라인 `<span>` 으로만 있다. 꺼내면서
      `min-w-[calc(var(--spacing-control-md)*2)]` 규칙을 같이 옮긴다(안 옮기면
      64px 통일이 미리보기에만 남는다). `status-*` 토큰 16개가 여기서 살아난다.
- [ ] **Chip** — 고를 수 있는 태그. Badge 에 클릭을 붙이는 게 아니라 Toggle 계열이다
      (포커스·키보드·checked·폼 값이 필요하다). ⓐ폼 제출용(CheckboxGroup 기반)과
      ⓑ즉시 반영용(ToggleGroup 기반) **둘 다 필요**하다고 확인됨.
      알약 모양은 `lib/styles.ts` 에 `CHIP` 으로 한 번 정의해 둘이 같이 쓴다
      (`SELECTION_BOX` 를 Checkbox·Radio 가 공유하는 구조와 같게).
- [ ] 탭 박스/버튼형 변형 + 탭 옆 배지 (Base UI Tabs 그대로, 스타일만)
- [ ] 입력칸 underlined 변형 (`FIELD_CONTROL` 변형)
- [ ] 버튼 아이콘 스토리 — `iconOnly` 는 이미 동작한다. 없는 건 예시뿐이다
- [ ] Spinner / Alert / Card

### 라이브러리 결정이 필요한 것

- [ ] **Calendar / DatePicker** — Base UI 1.8.0 에 없다(직접 확인).
      후보: `@rehookify/datepicker` 6.6.8(의존성 0, 훅만) · `react-day-picker` 10
      (자기 DOM 을 그림, date-fns 필요) · react-aria(세그먼트 입력칸까지 주지만
      `react-aria`+`react-stately` 전체를 끌고 온다).
      추천은 rehookify + `@internationalized/date`(단독 사용 가능) — 이 리포의
      "헤드리스가 동작, 우리가 토큰" 방식과 같다.
- [ ] Resizable / SplitPane, Tree — workbench(FRS) 에 필요할 텐데 Base UI 에 없다

### 새 라이브러리를 들이기 전에 반드시 고칠 것

`scripts/build-registry.mjs:154` 의 의존성 추출기가 **`@base-ui/react` 만 찾는다.**

```js
const npm = [...code.matchAll(/from '(@base-ui\/react)\/[a-z-]+'/g)]...
```

다른 패키지를 import 하면 레지스트리 JSON 의 `dependencies` 에 안 들어가고,
바깥 제품이 `npx shadcn add calendar` 했을 때 **설치되지 않은 패키지를 import 하는
파일**을 받는다. lucide-react 는 186 줄 프리셋 `dependencies` 에 손으로 박아 넘겼다.

### 그 밖에 빠진 것 (안 쓰이는 계약 토큰 36개가 가리킨다)

Pagination · Breadcrumb · EmptyState(지금 `TableEmpty` 가 표 안에만 갇혀 있다) ·
List(갤러리가 `<li>` 를 직접 그린다) · 멀티셀렉트(`combobox.tsx:33` 이 `multiple` 을
`Omit` 한다) · Code/Kbd(`--font-mono`) · 페이지 제목(`--text-display`,
`--text-heading-lg`)

> `npm run token-usage` 가 "36개 미사용"이라고 하지만 `--focus-ring-width`,
> `--focus-ring-offset` 은 오탐이다 — `styles.css:40` 의 `ax-focus-ring` 에서
> 쓰는데 스크립트가 CSS 를 안 훑는다.

## 작업 환경

```
cd ~/Documents/GitHub/midas-design-system && nvm use   # node 26 (.nvmrc)
npm run storybook                                      # 테마 랩
npm run verify                                         # 끝에 반드시
npm run token-usage -- --spacing-stack-xs              # 토큰 쓰는 곳
```
